# 依赖完成情况说明表 (Dependencies Status)

针对项目轻量化、本地化的需求，依赖框架采取了最为干练、直接的技术栈：

## 一、后端环境 (Server)

目录：`/server` 

| 依赖包名称      | 版本要求   | 核心用途               | 安装状态 |
|---------------|----------|----------------------| :--: |
| `express`      | `^4.18.2` | 极简路由控制与静态资源分发  |  ✅   |
| `cors`         | `^2.8.5`  | 允许前后端分离模式下的跨域  |  ✅   |
| `better-sqlite3`| `^9.4.0`  | **[核心]** 高性能本地 SQL 存储引擎，替代旧版 LowDB |  ✅   |
| `multer`       | `^1.4.5`  | 处理图片上传至 `assets/raw` 目录 |  ✅   |

后端已完成从 `lowdb` (JSON文件) 到 `SQLite` (全量物理表) 的平滑升级，通过内存事务保障了标注的高频并发写入稳定性。

## 二、前端环境 (Client)

目录：`/client` 

| 依赖包名称            | 版本要求   | 核心用途                          | 安装状态 |
|---------------------|----------|---------------------------------| :--: |
| `react`             | `^18.2.0`| 核心页面驱动渲染                      |  ✅   |
| `react-dom`         | `^18.2.0`| Web 呈现控制                        |  ✅   |
| `vite`              | `^6.0.0` | 现代化的极限加速构建流与开发服务器      |  ✅   |
| `tailwindcss`       | `^3.4.0` | 通过原子化 CSS 快速推进 UI 标准，放弃重组件库 |  ✅   |
| `lucide-react`      | `latest` | 提供美观的 SVG 控制图标和标识          |  ✅   |
| `clsx` / `tailwind-merge` | `latest` | 动态样式类名管理与整合            |  ✅   |

前端特意舍弃了 `Redux` 或重型组件包(如 MUI, AntD 等)，所有的页面逻辑通过极简的文件与路由驱动，贯彻《反引力解耦》设计思想。

---

> 备注：所有的依赖已在 `/Users/kwangwah/Code/SafeEYE/` 相关目录下通过 `npm` 全数初始化完成，在执行 `npm run dev` 即可开始本地联调。
